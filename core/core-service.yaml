apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: netskope-ce-core-service
  name: netskope-ce-core-service
  namespace: <NAMESPACE_NAME>
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 8000
  selector:
    app.kubernetes.io/name: core
  type: ClusterIP
